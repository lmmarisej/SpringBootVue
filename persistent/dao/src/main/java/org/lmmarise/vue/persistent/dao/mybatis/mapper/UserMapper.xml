<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.lmmarise.vue.persistent.dao.mybatis.mapper.UserMapper">
    <select id="loadUserByUsername" resultType="org.lmmarise.vue.domain.User">
        select *
        from user
        where username = #{username}
    </select>

    <select id="getUserRolesByUid" resultType="org.lmmarise.vue.domain.Role">
        select *
        from role r,
             user_roles ur
        where r.id = ur.roles_id
          and ur.user_id = #{id}
    </select>
</mapper>